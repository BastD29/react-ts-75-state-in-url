type ArticleType = {
  id: number;
  name: string;
  category: string;
  price: number;
  inStock: boolean;
};

// type QueryParamsType = {
//   [key: string]: string;
// };

export type { ArticleType /* QueryParamsType */ };
